<div id="<%= dom_id category %>">
  <%= category.name %>
  <div>
      <%= link_to t('categories.new.title'), edit_category_path(category), class: 'btn btn-warning'%>
      <%= button_to t('products.destroy.delete'), category_path(category), method: :delete, class: 'btn btn-danger'%>
  </div>

</div>
